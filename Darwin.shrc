# Mac specific settings that apply to both Bash and Zsh

[[ -f ~/profile/common.sh ]] && source ~/profile/common.shrc

# get full path of a file
alias y='myshowpath'

# ignore .DS_Store file
export FIGNORE=DS_Store

export CLICOLOR=1
export CVS_RSH=ssh
alias la="\ls -GAl"       # show hidden files
alias lt="\ls -Gltc"      # sort by change time
alias lr="\ls -GlR"       # recursive ls
alias ls="\ls -GlhS"      # sort by size
